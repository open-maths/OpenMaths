# OpenMaths

Read [AGENTS.md](AGENTS.md) — it is the complete contribution specification for this repository, and it applies to you.

Quick validation before opening a PR:

```bash
python3 -m venv .venv && .venv/bin/pip install pyyaml jsonschema
.venv/bin/python scripts/validate.py
```
